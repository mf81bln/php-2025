<?php $this->layout('layout::default') ?>

<!-- Stats Cards -->
<div class="row">
    <div class="col-lg-3 col-6">
        <div class="small-box text-bg-primary">
            <div class="inner">
                <h3><?= count($this->contacts) ?></h3>
                <p>Kontakte gesamt</p>
            </div>
            <div class="small-box-icon">
                <i class="fas fa-users"></i>
            </div>
        </div>
    </div>
    <div class="col-lg-3 col-6">
        <div class="small-box text-bg-success">
            <div class="inner">
                <h3><?= count(array_filter(iterator_to_array($this->contacts), fn($c) => $c->email)) ?></h3>
                <p>Mit E-Mail</p>
            </div>
            <div class="small-box-icon">
                <i class="fas fa-envelope"></i>
            </div>
        </div>
    </div>
    <div class="col-lg-3 col-6">
        <div class="small-box text-bg-warning">
            <div class="inner">
                <h3><?= count(array_filter(iterator_to_array($this->contacts), fn($c) => $c->phone)) ?></h3>
                <p>Mit Telefon</p>
            </div>
            <div class="small-box-icon">
                <i class="fas fa-phone"></i>
            </div>
        </div>
    </div>
    <div class="col-lg-3 col-6">
        <div class="small-box text-bg-info">
            <div class="inner">
                <h3><i class="fas fa-plus"></i></h3>
                <p>Neuer Kontakt</p>
            </div>
            <div class="small-box-icon">
                <i class="fas fa-user-plus"></i>
            </div>
            <a href="/contact/create" class="small-box-footer">
                Jetzt anlegen <i class="fas fa-arrow-circle-right"></i>
            </a>
        </div>
    </div>
</div>

<!-- Contact List Card -->
<div class="card">
    <div class="card-header">
        <h3 class="card-title">
            <i class="fas fa-address-book me-2"></i>
            Alle Kontakte
        </h3>
        <div class="card-tools">
            <a href="/contact/create" class="btn btn-success btn-sm">
                <i class="fas fa-plus me-1"></i> Neuer Kontakt
            </a>
        </div>
    </div>
    <div class="card-body p-0">
        <?php if (count($this->contacts) === 0): ?>
            <div class="alert alert-info m-3">
                <i class="fas fa-info-circle me-2"></i>
                Noch keine Kontakte vorhanden.
                <a href="/contact/create" class="alert-link">Ersten Kontakt anlegen</a>
            </div>
        <?php else: ?>
            <table class="table table-striped table-hover mb-0">
                <thead>
                    <tr>
                        <th style="width: 50px">#</th>
                        <th>Name</th>
                        <th>E-Mail</th>
                        <th>Telefon</th>
                        <th>Erstellt</th>
                        <th style="width: 180px">Aktionen</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($this->contacts as $contact): ?>
                        <tr>
                            <td>
                                <span class="badge text-bg-secondary"><?= $this->escapeHtml($contact->id) ?></span>
                            </td>
                            <td>
                                <i class="fas fa-user text-muted me-2"></i>
                                <strong><?= $this->escapeHtml($contact->name) ?></strong>
                            </td>
                            <td>
                                <?php if ($contact->email): ?>
                                    <a href="mailto:<?= $this->escapeHtml($contact->email) ?>">
                                        <i class="fas fa-envelope text-muted me-1"></i>
                                        <?= $this->escapeHtml($contact->email) ?>
                                    </a>
                                <?php else: ?>
                                    <span class="text-muted">-</span>
                                <?php endif; ?>
                            </td>
                            <td>
                                <?php if ($contact->phone): ?>
                                    <a href="tel:<?= $this->escapeHtml($contact->phone) ?>">
                                        <i class="fas fa-phone text-muted me-1"></i>
                                        <?= $this->escapeHtml($contact->phone) ?>
                                    </a>
                                <?php else: ?>
                                    <span class="text-muted">-</span>
                                <?php endif; ?>
                            </td>
                            <td>
                                <small class="text-muted">
                                    <i class="fas fa-clock me-1"></i>
                                    <?= $contact->created_at ? date('d.m.Y H:i', strtotime($contact->created_at)) : '-' ?>
                                </small>
                            </td>
                            <td>
                                <div class="btn-group">
                                    <a href="/contact/edit/<?= $contact->id ?>" class="btn btn-primary btn-sm" title="Bearbeiten">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a href="/contact/delete/<?= $contact->id ?>" class="btn btn-danger btn-sm" title="Löschen">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        <?php endif; ?>
    </div>
</div>
