<?php $this->layout('layout::default') ?>

<div class="row">
    <div class="col-lg-6">
        <div class="card card-warning">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-edit me-2"></i>
                    Kontakt bearbeiten
                </h3>
            </div>
            <form method="post" action="/contact/edit/<?= $this->id ?>">
                <div class="card-body">
                    <?php if ($this->error): ?>
                        <div class="alert alert-danger">
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            <?= $this->escapeHtml($this->error) ?>
                        </div>
                    <?php endif; ?>

                    <div class="mb-3">
                        <label for="name" class="form-label">
                            <i class="fas fa-user text-muted me-1"></i> Name <span class="text-danger">*</span>
                        </label>
                        <input type="text" class="form-control" id="name" name="name"
                               value="<?= $this->escapeHtml($this->data['name']) ?>"
                               placeholder="Vollständiger Name" required>
                    </div>

                    <div class="mb-3">
                        <label for="email" class="form-label">
                            <i class="fas fa-envelope text-muted me-1"></i> E-Mail
                        </label>
                        <input type="email" class="form-control" id="email" name="email"
                               value="<?= $this->escapeHtml($this->data['email'] ?? '') ?>"
                               placeholder="email@beispiel.de">
                    </div>

                    <div class="mb-3">
                        <label for="phone" class="form-label">
                            <i class="fas fa-phone text-muted me-1"></i> Telefon
                        </label>
                        <input type="tel" class="form-control" id="phone" name="phone"
                               value="<?= $this->escapeHtml($this->data['phone'] ?? '') ?>"
                               placeholder="+49 123 456789">
                    </div>

                    <div class="mb-3">
                        <label for="notes" class="form-label">
                            <i class="fas fa-sticky-note text-muted me-1"></i> Notizen
                        </label>
                        <textarea class="form-control" id="notes" name="notes" rows="4"
                                  placeholder="Zusätzliche Informationen..."><?= $this->escapeHtml($this->data['notes'] ?? '') ?></textarea>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-warning">
                        <i class="fas fa-save me-1"></i> Aktualisieren
                    </button>
                    <a href="/contact" class="btn btn-secondary">
                        <i class="fas fa-times me-1"></i> Abbrechen
                    </a>
                </div>
            </form>
        </div>
    </div>

    <div class="col-lg-6">
        <div class="card card-secondary">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-info-circle me-2"></i>
                    Kontakt-Info
                </h3>
            </div>
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-4">ID:</dt>
                    <dd class="col-sm-8"><span class="badge text-bg-secondary"><?= $this->id ?></span></dd>

                    <dt class="col-sm-4">Erstellt:</dt>
                    <dd class="col-sm-8">
                        <?= isset($this->data['created_at']) ? date('d.m.Y H:i', strtotime($this->data['created_at'])) : '-' ?>
                    </dd>
                </dl>
            </div>
            <div class="card-footer">
                <a href="/contact/delete/<?= $this->id ?>" class="btn btn-danger btn-sm">
                    <i class="fas fa-trash me-1"></i> Kontakt löschen
                </a>
            </div>
        </div>
    </div>
</div>
