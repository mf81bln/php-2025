<?php $this->layout('layout::default') ?>

<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="card card-danger">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-trash me-2"></i>
                    Kontakt löschen
                </h3>
            </div>
            <div class="card-body">
                <div class="alert alert-warning">
                    <h5><i class="fas fa-exclamation-triangle me-2"></i> Achtung!</h5>
                    Sie sind dabei, den folgenden Kontakt endgültig zu löschen.
                    Diese Aktion kann nicht rückgängig gemacht werden.
                </div>

                <div class="card bg-light">
                    <div class="card-body">
                        <div class="d-flex align-items-center">
                            <div class="me-3">
                                <span class="fa-stack fa-2x">
                                    <i class="fas fa-circle fa-stack-2x text-secondary"></i>
                                    <i class="fas fa-user fa-stack-1x fa-inverse"></i>
                                </span>
                            </div>
                            <div>
                                <h5 class="mb-1"><?= $this->escapeHtml($this->contact->name) ?></h5>
                                <?php if ($this->contact->email): ?>
                                    <p class="mb-0 text-muted">
                                        <i class="fas fa-envelope me-1"></i>
                                        <?= $this->escapeHtml($this->contact->email) ?>
                                    </p>
                                <?php endif; ?>
                                <?php if ($this->contact->phone): ?>
                                    <p class="mb-0 text-muted">
                                        <i class="fas fa-phone me-1"></i>
                                        <?= $this->escapeHtml($this->contact->phone) ?>
                                    </p>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <form method="post" action="/contact/delete/<?= $this->id ?>" class="d-inline">
                    <button type="submit" name="confirm" value="yes" class="btn btn-danger">
                        <i class="fas fa-trash me-1"></i> Ja, endgültig löschen
                    </button>
                </form>
                <a href="/contact" class="btn btn-secondary">
                    <i class="fas fa-arrow-left me-1"></i> Abbrechen
                </a>
            </div>
        </div>
    </div>
</div>
